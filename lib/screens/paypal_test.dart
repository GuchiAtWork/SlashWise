import 'package:flutter/material.dart';

class PaypalTest extends StatelessWidget {
  static const routeName = "/paypal_test";
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("PAY MEEEE")
    );
  }
}